# Martin tile server configuration.
# Connection string is provided via DATABASE_URL env var in docker-compose.yml.
# Explicit function allowlist — only listed functions are served.
# Add new entries here when creating new tile function sources.

postgres:
  connection_string: ${DATABASE_URL}
  functions:
    network_objects:
      schema: network_model
      function: network_objects
